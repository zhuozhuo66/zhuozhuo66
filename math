#include <stdio.h>
//get_max
int get_max(int x, int y)
{
	return (x > y) ? (x) : (y);
}
int main()
{
	int a = 0;
	int b = 0;
	scanf("%d %d", &a, &b);
	int max = get_max(num1, num2);
	printf("max = %d\n", max);
	return 0；
}
